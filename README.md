# threading-flac-to-mp3

simple flac to mp3 converter

you have to have ffmpeg installed and added to PATH

run main.py, then choose input and output directories

overwrite files is set to answer NO by default
